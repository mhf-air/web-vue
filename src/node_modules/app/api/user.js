const { ax } = require("./aa-lib.js")
const apiRoute = require("../api-route.js") // 接口路由

const EnumUser = {
  gender: {
    title: "性别",
    value: {
      male: {
        title: "男",
      },
      female: {
        title: "女",
      },
    },
  },
}

module.exports = {

  ListUser(postData) {
    return ax(apiRoute.ListUser, postData)
  },

  GetUser(postData) {
    return ax(apiRoute.GetUser, postData)
  },

  Login(postData) {
    return ax(apiRoute.Login, postData)
  },

  Logout(postData) {
    return ax(apiRoute.Logout, postData)
  },

  EnumUser,

}
